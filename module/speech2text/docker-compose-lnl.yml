services:
  stt:
    build:
      context: .
      args:
        HTTP_PROXY: ${HTTP_PROXY:-}
        HTTPS_PROXY: ${HTTPS_PROXY:-}
        NO_PROXY: ${NO_PROXY:-}
      dockerfile: Dockerfile.lnl
    image: speech2text
    container_name: stt
    restart: always
    ports:
      - "9000:9000"
    environment:
      - HF_HOME=/app/hf_home
    env_file:
      - whisper.env
    devices:
      - /dev/dri
    volumes:
      - ./.cache/stt/hf_home:/app/hf_home
    shm_size: 32G
    deploy:
      resources:
        limits:
          memory: 32G